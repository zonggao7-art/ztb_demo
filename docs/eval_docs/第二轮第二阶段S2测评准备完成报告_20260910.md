# 第二轮第二阶段 S2 测评准备完成报告

检查日期：2026-09-10。

## 结论

S2 已具备正式启动条件。正式200题采集和付费Ragas评分尚未启动，等待人工发出开始指令。

本阶段被固定解释为 **BM25＋RRF＋DeepSeek官方API整体升级测评**。由于检索路线和回答服务同时改变，结果只能说明整套S2方案相对S1的变化，不能把差异单独归因给BM25、RRF或API供应商。

## 已完成门禁

| 项目 | 固定结果 |
|---|---|
| 题集 | 200题；单跳130、多跳70；SHA256为 `D0960557FED978D2CA1D7B47BBAD77A513BAA65F24F6F49C6FF62F880823E87A` |
| S1基线 | `s1_20260910_formal_02`；六个核心结果文件已冻结SHA256 |
| S2执行源码 | 47个执行、测评、运行时适配和依赖锁文件已冻结SHA256；不包含`.env`和密钥 |
| 知识库 | `public_kb`，920条；题集164个唯一chunk、270次关联全部存在且正文一致 |
| 稠密检索 | BGE-M3，Top30，COSINE，nprobe=32 |
| 稀疏检索 | Milvus服务端BM25，Top30；`text -> sparse_vector` Function与稀疏索引已完成 |
| 融合 | RRF，k=60，融合候选30，最终Top5 |
| S2排除项 | 不调用reranker；不启用动态过滤；不对RRF分数应用0.45余弦阈值 |
| 回答模型 | `deepseek-flash`，DeepSeek官方API，temperature=0，timeout=60秒，SDK max_retries=1 |
| 回答推理模式 | 所有官方DeepSeek请求在最终请求载荷层强制 `thinking.type=disabled` |
| 裁判 | MiniMax-M3；关闭thinking；最大输出4096；评分嵌入仍为BGE-M3 |
| 正式采集 | 文件顺序、单题串行、每题端到端总时限600秒 |
| 正式评分 | 四项指标独立评分；4题并发；每题每项600秒；支持断点续跑 |
| 结果隔离 | 使用新运行编号和目录；已有目录拒绝覆盖 |
| 统计能力 | 六项核心指标、精确chunk命中、单跳/多跳分层、逐题S1/S2配对差值 |

S2采集入口自身会重新执行题集、检索参数、回答模型、推理模式、知识库和S1基线门禁；不能仅靠一份旧的预检报告绕过检查。评分入口也会重新核对题集、S1快照、裁判模型、并发和超时口径。

最终离线验证结果：专项测试46项通过；项目完整测试632项通过、1项按原设计跳过；依赖检查119个包全部兼容；最终证据哈希与运行计划一致。

## 真实预演

使用2道单跳和2道多跳题，严格顺序调用完整S2 `knowledge_qa`：

- 4/4成功；
- 每题均返回5条引用；
- 没有reranker调用；
- 回答模型usage均来自官方接口实际返回；
- 单题耗时为1.665～1.886秒；
- 6个标准chunk命中5个，4题均至少命中1个，3题全部命中。

该结果只证明链路可运行，不是正式质量或速度结论，也不进入正式成绩。

## 最终验证

| 检查 | 结果 |
|---|---:|
| S2与DeepSeek定向测试 | 45项通过 |
| 项目完整测试 | 631项通过，1项跳过 |
| Python依赖一致性 | 119个包兼容 |
| 最终S2预检 | 通过 |
| 正式S2运行目录 | 不存在，尚未启动 |

## 新增诊断与报告

每道S2结果会保存 `retrieval_diagnostics`：标准chunk、最终Top5 chunk、命中与缺失ID、命中排名、精确Macro/Micro Recall以及多跳题是否全部命中。

正式报告会分别汇总单跳和多跳的质量、P50/P95与精确命中。S2采集完成后自动生成 `paired_comparison.json` 和 `paired_comparison.md`，逐题核对相同ID、顺序、问题、题型和标准chunk后才允许比较；评分完成后同一报告自动补齐四项质量差值。

## S1历史限制

S1清单记录 `working_tree_dirty=true`。现已冻结S1结果证据，但无法事后恢复S1当时未提交代码的精确状态。该限制不会被技术手段伪装成已解决；本次是在“接受S1正式结果”的人工决定下继续整体升级比较。

## 调用量与时间准备

- 正式采集固定200次查询嵌入和200次回答模型调用。
- 正式评分计划产生800项指标结果；内部裁判调用次数由Ragas决定。S1曾产生2454次裁判调用和400次评分嵌入调用，仅作为预算参考。
- S1系统Token为1,157,296，评分Token为7,563,978。S2关闭推理后可能降低回答Token和耗时，但不得在正式结果前预设降幅。
- 单题采集与单项评分均有600秒超时和断点文件，不会因单项失败删除其他结果。

## 正式运行编号与命令

预留运行编号：`s2_20260910_formal_01`。检查时该目录不存在，正式命令会原子创建，避免覆盖旧结果。

```powershell
# 正式采集：会调用真实查询嵌入和DeepSeek回答模型
.\.venv-eval\Scripts\python.exe scripts\run_rag_eval.py collect --stage s2 --run-id s2_20260910_formal_01 --confirm-live-run

# 仅在200题采集完整后启动：会调用MiniMax裁判和评分嵌入
.\.venv-eval\Scripts\python.exe scripts\run_rag_eval.py score --run-dir second_round_eval_data\runs\s2_20260910_formal_01 --confirm-paid-scoring
```

机器可读的运行计划位于 `second_round_eval_data/s2_run_plan.json`；S2执行源码快照位于 `second_round_eval_data/s2_source_snapshot.json`；最终预检位于 `second_round_eval_data/s2_preflight_report.json`；真实预演位于 `second_round_eval_data/smoke_runs/s2_preflight_20260910_01/`。
