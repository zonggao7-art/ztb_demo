# A2 界面检查记录

2026-09-15，本地静态演示；不是实际后端或公网验收。

## 自动检查

- `npm.cmd run build`：TypeScript 检查、Vite 生产构建通过。
- `npm.cmd test`：5 项状态测试通过。覆盖停止后的迟到事件、完整答案校准、请求隔离、重复发送、失败后重试保留草稿。

## 浏览器检查

通过 Codex 浏览器自动化在 1280 × 720 桌面窗口和 390 × 844 手机视口检查：

- 首页示例选择、输入与发送；空输入时按钮禁用。
- 请求期间可以编辑下一条草稿，发送禁用，停止按钮可用。
- 停止后的状态提示正确，草稿保留；重试可以完成新一轮。
- 法规回答展示两条虚构引用；正文来源编号可以展开并定位对应引用。
- 中标表格返回一条虚构记录，显示金额单位未核定提示。
- 手机表格在自身区域横向滚动，650px 表格内容包含在约 316px 区域内；页面没有因此整体横向溢出。
- 手机会话菜单可打开，新建会话后关闭菜单。
- 服务错误有明确未完成提示；重试后成功，原失败轮保留。
- 复制成功后按钮显示“已复制”。
- 正文后中断时保留已收到文字，明确标注“结果接收中断”，不显示成功。
- 检查过程中未发现页面控制台 error / warn。

发现并修复：首页首次打开被自动滚到底部；未收到引用详情时正文标记不再表现为可点击按钮。

## 尚未验证的范围

- 手机视口检查不等于真机键盘、Safari 或所有浏览器兼容性测试。
- 完整 Markdown、真实 SSE 分帧、真实后端取消、鉴权、持久化和公网部署未实现。
- 需要补充条件、部分完成、超时等预览已提供，但不代表当前真实后端有对应且可区分的输出语义。
- 未进行正式可访问性审计、性能负载测试或跨浏览器自动化。

# A3 真实接口联调记录

2026-09-15。本节为 A3 的新增验收；上方 A2 的未实现范围是当时的历史记录。

## 自动检查

- `npm.cmd test`：18 项通过（原有 5 项状态测试 + 13 项协议及业务分类测试）。
- `npm.cmd run build`：TypeScript 与生产构建通过。
- 覆盖 UTF-8 逐字节分片、CRLF / CR、多帧、注释、多行 data、截断与非法信封、跨请求事件、缺失 final、HTTP / 协议错误、取消读取与终态释放。
- 覆盖工商、经营范围、处罚、两类中标工具的 step_id 分类；空结果与失败区分；孤立及重复关联；缺失字段与数值 0；业务证据不足与引用校验。

## 真实浏览器链路

- 通过页面发送法规问题，经 Vite 代理到真实 FastAPI，返回正文与 5 条真实引用，首次页面耗时约 19.4 秒。
- 点击正文来源编号能展开引用原文和片段标识。
- 再次发送后点击停止：页面显示“已停止”；后端日志记录 SSE client disconnected（约 3.1 秒）。这验证 HTTP 断开，不证明远端模型计费即时终止。
- 停止期间编辑的草稿保留，点击重试后同一问题成功返回（约 4.6 秒），原停止轮和草稿保留。两次耗时为本次观测，不是性能承诺。
- 当前未发现浏览器 error / warn。

## 新增展示检查

- 桌面查看企业键值卡片；手机 390 × 844 视口检查经营范围长文本、处罚表格和空结果 / 失败提示。
- 手机处罚表格 800px 内容位于 311px 滚动容器内，页面宽度未超过视口。
- “界面演示 → 企业资料与处罚”同时展示工商、经营范围、处罚、精确空查询与失败查询，均标注虚构。
- 从演示切换真实问答会创建新会话并清空演示草稿。检查后恢复真实问答首页。

## 验证边界

- 真实联调使用法规问答；SQL 各类结果通过契约样例与虚构业务展示检查，未逐类调用真实业务数据库。
- 未验证远端计费停止、生产反向代理、鉴权、持久化、负载或公网访问。
- 当前仍为本机地址；A3 接口接通不等于公网部署完成。


## 六模块首页更新（2026-09-15）

- 真实首页展示六个功能模块及已确认的预设问题；虚构演示入口保持原有样例。
- 浏览器逐一点击六张卡片：输入框文字均与对应预设问题一致，点击后对话轮数仍为 0，没有自动发送。
- 1280 × 720 桌面视口，两行三列的六张卡片可同时看到；较矮屏幕使用紧凑排版。
- 390 × 844 手机视口使用单列，项目编号完整换行；页面宽度 390px，无横向溢出。
- 本次仅改变入口与布局，未重新调用真实 Agent；数据库样例由前一轮只读精确查询验证。
- TypeScript 检查与生产构建通过。

## 浏览器保存更新（2026-09-15）

- 自动测试合计 25 项通过；新增 7 项覆盖恢复完整数据、运行中断恢复、坏数据与版本不匹配、配额 / 权限错误、跨标签页冲突、清空及迟到事件隔离。
- TypeScript 与生产构建通过。
- 使用独立地址 127.0.0.1:5174 的生产预览，避免修改正常使用地址的历史。
- 浏览器关闭测试页后重新打开：演示模式、选中会话、完整业务分组及草稿恢复。
- 请求开始后立即刷新：原轮恢复为“结果接收中断”，没有卡在运行中或自动重发。
- 点击清空记录出现确认说明，点击保留记录后原会话仍在。实际清空写入和迟到回调隔离通过内存存储测试验证，没有在用户浏览器执行清空。
- 两个测试标签页：旧页面收到保存暂停提示；尝试编辑后刷新，读到另一页最新草稿，未覆盖它。
- 测试页控制台未发现 error / warn。
- 保存仅在相同浏览器配置、相同站点地址内有效。没有实现服务端历史、跨设备同步、多页面编辑合并或 Agent 多轮上下文。


## 六模块真实端到端验收（2026-09-15）

本节补足此前仅检查 SQL 样例和虚构组件的验证范围。使用 127.0.0.1:5173 页面，经 Vite 代理访问本机 8000 的现有 FastAPI / unified Agent，依次实际发送首页六个问题，再补一条一般对话。每题新建会话，均使用真实问答模式。

| 样例 | 页面终态 | 本次耗时 | 观测结果 |
| --- | --- | --- | --- |
| 法规知识问答 | 本轮结果已返回 | 7.8 秒 | 正文、5 条引用；正文来源按钮能展开原文 |
| 企业工商信息 | 本轮结果已返回 | 13.7 秒 | 1 条工商卡片；名称与预设企业一致 |
| 企业经营范围 | 本轮结果已返回 | 2.3 秒 | 1 条经营范围长文本卡片 |
| 企业处罚记录 | 本轮结果已返回 | 1.8 秒 | 1 条处罚表格；日期、行为、结果、执法单位正常展示 |
| 项目中标详情 | 本轮结果已返回 | 1.7 秒 | 项目编号精确识别；1 条中标表格；金额原值和单位边界保留 |
| 企业中标历史 | 本轮结果已返回 | 2.9 秒 | 按供应商返回1条历史记录，与预设项目相互对应 |
| 一般对话 | 本轮结果已返回 | 1.3 秒 | 返回能力介绍，不附加无关业务表格 |

- 7 / 7 本次请求到达正常业务终态。五类 SQL 展示与预设的企业、项目相对应，没有串入其他会话内容。
- 法规正文来源编号点击后可展开原文；本次检查引用呈现和关联，不等于对所有法规结论进行法律审定。
- 项目编号中的方括号完整保留；企业中标历史按中标供应商呈现。金额原始值及单位未核定说明保留。
- 刷新页面后七条会话恢复，切回项目中标会话后表格仍完整，页面显示已保存到此浏览器。
- 本次浏览器控制台未发现 error / warn。测试会话保留供手动回看；未清空原有记录。
- 此次没有修改前端或 Agent 业务代码，只记录验证结果。
- 耗时是单次观测，不是性能基准或稳定性保证。尚未据此验证所有输入、异常场景、并发、跨轮上下文、飞书或公网部署。

## A6：真实 HTTP 断流与下一轮恢复（2026-09-16）

- 构建现有前端通过；未修改产品前端、后端或 Agent 业务代码。
- 使用临时本地服务 `http://localhost:5174/` 承载生产构建，与常用 127.0.0.1:5173 和之前 127.0.0.1:5174 的浏览器记录隔离；未清空任何既有历史。
- 仅对 `[A6断流验收]` 开头的问题注入：合法 SSE meta、stage、带“断流验收模拟片段”标记的 token；随后在 HTTP chunk 未传完时关闭连接，不发送 final/error/cancelled。
- 浏览器通过真实 fetch / 流解析路径处理故障，约 2.3 秒后显示“结果接收中断”和“无法连接或连接已中断”提示，保留模拟片段，退出运行状态并恢复重试入口。未使用界面演示模式。
- 刷新页面后，该轮仍标记中断，没有保存为成功答案。
- 同一会话下一轮发送真实项目编号查询，临时服务转发至现有 8000 后端；约 2.2 秒后显示“本轮结果已返回”及正确项目的一条中标表格，没有混入模拟片段。
- 再次刷新后，中断轮和成功轮仍分别保留正确状态及正文。
- 临时服务日志确认 `FAULT_INJECTED`、`REAL_BACKEND_REQUEST_FINISHED`，错误日志为空。
- 结论：本次受控的传输中断、半截内容持久化和下一轮恢复验收通过。不等同于操作系统断网、所有超时路径或公网链路已验证。
- 临时工具位于已忽略的 `logs/a6_stream_fault_server.py`，进程记录在 `logs/a6_stream_fault.pid`，验收页面暂保留供回看；常用网页与飞书连接服务未停止。
