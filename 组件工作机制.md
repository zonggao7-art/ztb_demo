# 招投标智能助手 — 组件工作机制图解

> 使用 Mermaid 最新语法，中文标注，全面展示每个组件的内部工作机制与数据流向。

---

## 目录

1. [系统全景架构](#1-系统全景架构)
2. [Agent 骨架 — StateGraph 工作流](#2-agent-骨架--stategraph-工作流)
3. [Router — 意图路由机制](#3-router--意图路由机制)
4. [Knowledge QA — 专业知识问答](#4-knowledge-qa--专业知识问答)
5. [Price Inquiry — 智能询价全链路](#5-price-inquiry--智能询价全链路)
6. [Price Inquiry — 多阶段召回与权重策略](#6-price-inquiry--多阶段召回与权重策略)
7. [Public KB — RAG 混合检索流水线](#7-public-kb--rag-混合检索流水线)
8. [Public KB — PDF 入库全流程](#8-public-kb--pdf-入库全流程)
9. [Answer Templates — 回答模板渲染引擎](#9-answer-templates--回答模板渲染引擎)
10. [Output Templates — 输出字段筛选框架](#10-output-templates--输出字段筛选框架)
11. [Fallback — 全局异常兜底机制](#11-fallback--全局异常兜底机制)
12. [Checkpointer — 对话记忆持久化](#12-checkpointer--对话记忆持久化)
13. [数据源协同策略](#13-数据源协同策略)

---

## 1. 系统全景架构

```mermaid
graph TB
    %% 样式定义
    classDef entryLayer fill:#1a1a2e,stroke:#e94560,stroke-width:2px,color:#eee,rx:15,ry:15
    classDef coreLayer fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef bizLayer fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12
    classDef dataLayer fill:#1a1a2e,stroke:#f4a261,stroke-width:2px,color:#ffd166,rx:12,ry:12
    classDef infraLayer fill:#2d1b00,stroke:#5c6370,stroke-width:1.5px,color:#abb2bf,rx:10,ry:10
    classDef externalAPI fill:#3d0a3d,stroke:#c77dff,stroke-width:2px,color:#e0b3ff,rx:10,ry:10
    classDef router fill:#3a0a3a,stroke:#ff6b6b,stroke-width:2px,color:#ffc0cb,rx:12,ry:12
    classDef guard fill:#2d1b00,stroke:#e76f51,stroke-width:2px,color:#ffc069,rx:10,ry:10

    %% 标题
    TITLE["🏗️ 招投标智能助手 — 系统全景架构"]
    class TITLE entryLayer

    %% 接入层
    subgraph L1["━━━ 接入层 ━━━"]
        direction LR
        CLI["🖥️ CLI 命令行<br/>python -m agent"]
        WEB["🌐 FastAPI 接口<br/>（规划中）"]
    end
    class L1 entryLayer

    %% Agent 核心层
    subgraph L2["━━━ LangGraph Agent 骨架 ━━━"]
        direction TB
        START((▶ START))
        ROUTER["🔀 Router<br/>LLM 意图分类器<br/>────────<br/>structured_output<br/>↓ 回退<br/>Tool Calling"]
        STATE[("📋 AgentState<br/>messages + router_intent<br/>+ business_result")]
        END_NODE((🏁 END))
    end
    class L2 coreLayer

    %% 业务插件层
    subgraph L3["━━━ 业务插件层 ━━━"]
        direction LR
        KQA["📚 专业知识问答<br/>knowledge_qa<br/>────────<br/>对接 Milvus RAG<br/>混合检索 + 拒答"]
        PI["💰 智能询价<br/>price_inquiry<br/>────────<br/>统一意图解析<br/>6级多阶段召回<br/>安全校验 + 模板渲染"]
        GC["💬 通用对话<br/>general_chat<br/>────────<br/>纯 LLM 闲聊<br/>功能引导"]
        DQ["📄 文档问答<br/>doc_qa<br/>────────<br/>占位节点<br/>功能待上线"]
        FB["🔄 兜底引导<br/>fallback<br/>────────<br/>意图不明引导<br/>异常降级接管"]
    end
    class L3 bizLayer

    %% 防护层
    subgraph L4["━━━ 安全防护层 ━━━"]
        FALLBACK_WRAP["🛡️ _with_fallback 装饰器<br/>所有业务节点统一包裹<br/>单点故障不中断整体流程"]
        GUARDS["🔒 多层安全校验<br/>P0-1 编号特征识别<br/>P0-11 工商主体名校验<br/>P0-12 项目编号正则兜底<br/>前置拦截 + 后置回溯"]
    end
    class L4 guard

    %% 基础设施层
    subgraph L5["━━━ 基础设施层 ━━━"]
        direction LR
        MILVUS[("🐘 Milvus<br/>向量数据库<br/>1024维 COSINE<br/>IVF_FLAT nlist=128")]
        MYSQL[("🐬 MySQL ztb_clean<br/>4张核心表<br/>FULLTEXT ngram<br/>连接池 max=8")]
        MEM[("💾 Checkpointer<br/>MemorySaver<br/>→ SQLite → PG → Redis")]
    end
    class L5 infraLayer

    %% 外部 API
    subgraph L6["━━━ 外部服务 ━━━"]
        direction LR
        DEEPSEEK[("🤖 DeepSeek API<br/>deepseek-chat<br/>temperature=0")]
        SILICON["("🧬 SiliconFlow API<br/>BGE-m3 Embedding<br/>BGE-reranker-v2-m3")]
        MINERU[("📑 MinerU API<br/>PDF → Markdown<br/>OCR 解析")]
    end
    class L6 externalAPI

    %% 连线
    CLI --> START
    WEB --> START
    START --> ROUTER
    STATE -.- ROUTER
    ROUTER --> KQA
    ROUTER --> PI
    ROUTER --> GC
    ROUTER --> DQ
    ROUTER --> FB
    KQA -.->|🛡️| FALLBACK_WRAP
    PI -.->|🛡️| FALLBACK_WRAP
    GC -.->|🛡️| FALLBACK_WRAP
    DQ -.->|🛡️| FALLBACK_WRAP
    KQA --> END_NODE
    PI --> END_NODE
    GC --> END_NODE
    DQ --> END_NODE
    FB --> END_NODE
    KQA -.-> MILVUS
    KQA -.-> DEEPSEEK
    PI -.-> MYSQL
    PI -.-> MILVUS
    PI -.-> DEEPSEEK
    GC -.-> DEEPSEEK
    MILVUS -.-> SILICON
    MILVUS -.-> SILICON
    PI -.-> GUARDS
```

---

## 2. Agent 骨架 — StateGraph 工作流

```mermaid
graph LR
    classDef start fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:50,ry:50
    classDef routerNode fill:#3a0a3a,stroke:#ff6b6b,stroke-width:2px,color:#ffc0cb,rx:15,ry:15
    classDef bizNode fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:15,ry:15
    classDef endNode fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:50,ry:50
    classDef stateNode fill:#1a3a5c,stroke:#1890ff,stroke-width:1.5px,color:#91caff,stroke-dasharray:5 5,rx:10,ry:10
    classDef wrapperNode fill:#2d1b00,stroke:#e76f51,stroke-width:1.5px,color:#ffc069,stroke-dasharray:3 3,rx:10,ry:10

    START((▶ START))
    ROUTER["🔀 router<br/>LLM 意图分类<br/>────────<br/>5 种意图枚举<br/>知识问答 | 智能询价<br/>通用对话 | 文档问答<br/>兜底引导"]
    STATE["📋 AgentState<br/>messages: 对话历史<br/>router_intent: 路由意图<br/>business_result: 业务负载"]

    subgraph BIZ["业务节点 — 统一包裹 _with_fallback"]
        direction LR
        KQA["📚 knowledge_qa"]
        PI_BU["💰 price_inquiry"]
        GC_BU["💬 general_chat"]
        DQ_BU["📄 doc_qa"]
    end

    FB["🔄 fallback"]
    END_NODE((🏁 END))

    START --> ROUTER
    STATE -.- ROUTER
    ROUTER -->|"knowledge_qa"| KQA
    ROUTER -->|"price_inquiry"| PI_BU
    ROUTER -->|"general_chat"| GC_BU
    ROUTER -->|"doc_qa"| DQ_BU
    ROUTER -->|"fallback"| FB
    KQA --> END_NODE
    PI_BU --> END_NODE
    GC_BU --> END_NODE
    DQ_BU --> END_NODE
    FB --> END_NODE

    class START startNode
    class ROUTER routerNode
    class STATE stateNode
    class KQA,PI_BU,GC_BU,DQ_BU bizNode
    class FB bizNode
    class END_NODE endNode
    class BIZ wrapperNode
```

### 工作流说明

| 步骤 | 组件 | 动作 |
|------|------|------|
| ① | `AgentGraph.invoke(question)` | 创建 HumanMessage，传入 StateGraph |
| ② | `router` | LLM 携带最近3轮历史进行意图分类 |
| ③ | `_route_by_intent(state)` | 条件边：根据 `router_intent` 分发到对应节点 |
| ④ | 业务节点 | 执行具体逻辑，返回 `business_result` + `AIMessage` |
| ⑤ | `_with_fallback` | 任何未捕获异常 → 自动降级为友好提示 |
| ⑥ | `END` | 输出 `{"answer", "intent", "business_result"}` |

---

## 3. Router — 意图路由机制

```mermaid
graph TB
    classDef input fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:12,ry:12
    classDef logic fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef decision fill:#3a0a3a,stroke:#ff6b6b,stroke-width:2px,color:#ffc0cb,rx:12,ry:12
    classDef output fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12
    classDef fallback fill:#2d1b00,stroke:#e76f51,stroke-width:2px,color:#ffc069,rx:12,ry:12
    classDef toolNode fill:#1a3a5c,stroke:#c77dff,stroke-width:1.5px,color:#e0b3ff,rx:10,ry:10

    subgraph INPUT["📥 输入"]
        MSG["用户消息 + 最近3轮对话历史<br/>_format_history(messages, max_turns=3)"]
    end
    class INPUT input

    subgraph CLASSIFY["🧠 LLM 意图分类 (temperature=0)"]
        direction TB
        PROMPT["System Prompt<br/>5种意图定义 + 路由示例<br/>ROUTER_SYSTEM_PROMPT"]
        STRUCT["方案A: with_structured_output<br/>→ RouterDecision Pydantic Model<br/>intent + reason + sub_intent"]
        TOOL["方案B: Tool Calling 回退<br/>bind_tools(5个虚拟工具)<br/>工具名即意图枚举值"]
        DETECT{"预检测:<br/>API 支持<br/>structured_output?"}
        RUNTIME{"运行时:<br/>structured<br/>调用成功?"}
    end
    class CLASSIFY logic

    subgraph TOOLS["🔧 5个虚拟路由工具"]
        direction LR
        T1["route_knowledge_qa"]
        T2["route_price_inquiry"]
        T3["route_general_chat"]
        T4["route_doc_qa"]
        T5["route_fallback"]
    end
    class TOOLS toolNode

    subgraph RESULT["📤 路由结果"]
        direction LR
        K["knowledge_qa<br/>专业知识问答"]
        P["price_inquiry<br/>智能询价"]
        G["general_chat<br/>通用对话"]
        D["doc_qa<br/>文档问答"]
        F["fallback<br/>兜底引导"]
    end
    class RESULT output

    FB_FINAL["🔄 最终降级 → fallback<br/>写入 state['router_intent']"]
    class FB_FINAL fallback

    MSG --> PROMPT
    PROMPT --> DETECT
    DETECT -->|"✅ 支持"| STRUCT
    DETECT -->|"❌ 不支持"| TOOL
    STRUCT --> RUNTIME
    RUNTIME -->|"✅ 成功"| RESULT
    RUNTIME -->|"❌ 异常"| TOOL
    TOOL -.- TOOLS
    TOOLS --> RESULT
    STRUCT -.->|"异常兜底"| FB_FINAL
    TOOL -.->|"异常兜底"| FB_FINAL
    RESULT -->|"写入 State"| FB_FINAL
```

### 路由枚举与触发条件

| 意图 | 枚举值 | 典型触发 |
|------|--------|---------|
| 专业知识问答 | `knowledge_qa` | "招标方式有哪些？""评标委员会怎么组成？" |
| 智能询价 | `price_inquiry` | "XX公司中标了什么？""查一下皮艺沙发的中标记录" |
| 通用对话 | `general_chat` | "你好""你能做什么？" |
| 文档问答 | `doc_qa` | "帮我分析这个文档"（Demo占位） |
| 兜底引导 | `fallback` | 意图无法确定时 |

---

## 4. Knowledge QA — 专业知识问答

```mermaid
graph TB
    classDef input fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:12,ry:12
    classDef lazy fill:#1a3a5c,stroke:#1890ff,stroke-width:2px,color:#91caff,rx:12,ry:12
    classDef rag fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef output fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12
    classDef error fill:#2d1b00,stroke:#e76f51,stroke-width:2px,color:#ffc069,rx:12,ry:12

    subgraph IN["📥 输入"]
        Q["从 AgentState 提取用户问题<br/>str(messages[-1].content)"]
    end
    class IN input

    subgraph INIT["🔌 惰性单例初始化"]
        direction TB
        CHECK{"_rag_engine<br/>已存在?"}
        CREATE["创建 PublicKnowledgeRAG()<br/>→ store_manager.load_existing()<br/>→ _build_qa_chain()"]
        CACHE["直接返回缓存实例"]
    end
    class INIT lazy

    subgraph RAG["🔍 PublicKnowledgeRAG.query()"]
        direction TB
        RAG_IN["传入问题文本"]
        RETRIEVE["LCEL 混合检索链<br/>━━━━━━━━━━<br/>稠密向量 COSINE k=30<br/>+ 稀疏向量 BM25 k=30<br/>→ RRF 融合 k=60 Top-30<br/>→ BGE-reranker-v2-m3 精排<br/>→ 自适应阈值过滤"]
        DECIDE{"检索结果<br/>为空?"}
        REFUSE["❌ 拒答<br/>'公共知识库中暂无相关内容'"]
        GENERATE["✅ LLM 生成回答<br/>prompt → deepseek-chat<br/>→ StrOutputParser<br/>附带引用来源"]
    end
    class RAG rag

    subgraph OUT["📤 输出"]
        ANSWER["business_result = {<br/>  branch: 'knowledge_qa',<br/>  answer: str,<br/>  data: { sources: [...] }<br/>}<br/>+ AIMessage"]
        ERR_OUT["business_result = {<br/>  branch: 'knowledge_qa',<br/>  answer: '知识库尚未初始化...',<br/>  data: { error: str }<br/>}"]
    end
    class OUT output
    class ERR_OUT error

    Q --> CHECK
    CHECK -->|"否"| CREATE
    CHECK -->|"是"| CACHE
    CREATE --> RAG_IN
    CACHE --> RAG_IN
    RAG_IN --> RETRIEVE
    RETRIEVE --> DECIDE
    DECIDE -->|"空"| REFUSE
    DECIDE -->|"有结果"| GENERATE
    REFUSE --> ANSWER
    GENERATE --> ANSWER
    CHECK -.->|"RuntimeError<br/>知识库未初始化"| ERR_OUT
```

---

## 5. Price Inquiry — 智能询价全链路

```mermaid
graph TB
    classDef step1 fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:12,ry:12
    classDef step2 fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef step3 fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12
    classDef step4 fill:#1a3a5c,stroke:#c77dff,stroke-width:2px,color:#e0b3ff,rx:12,ry:12
    classDef step5 fill:#2d1b00,stroke:#f4a261,stroke-width:2px,color:#ffd166,rx:12,ry:12
    classDef guard fill:#3d0a3d,stroke:#e76f51,stroke-width:2px,color:#ffc069,rx:12,ry:12
    classDef output fill:#1a1a2e,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12

    subgraph S1["📥 阶段1: 统一意图解析 _parse_unified_intent()"]
        direction TB
        Q1["用户问题"]
        LLM1["ChatOpenAI (temperature=0)<br/>_UNIFIED_INTENT_PROMPT<br/>───────<br/>一次性输出:<br/>• sub_route 二级路由<br/>• query_type 查询类型<br/>• hard_filters 过滤条件<br/>• semantic_keywords 语义关键词<br/>• exact_tokens 精确匹配"]
        PARSE["_extract_json()<br/>容错提取 JSON"]
        POST["_post_process_intent()<br/>去噪 + 归一化<br/>• _denoise_keywords()<br/>• _split_overlong_keyword()"]
        INTENT[("SearchIntent<br/>sub_route<br/>query_type<br/>hard_filters<br/>semantic_keywords")]
    end
    class S1 step1

    subgraph S2["🛡️ 阶段2: 多层安全校验"]
        direction TB
        G1{"product_query?"}
        G2{"project_detail<br/>无project_number?"}
        G3{"裸实体名<br/>无查询意图?"}
        G4{"未提取到<br/>有效实体?"}
        G5{"project_number<br/>确定性提取<br/>成功?"}
        PASS["✅ 通过所有校验"]
        REJECT["❌ 触发引导话术<br/>_build_unified_guidance()"]
    end
    class S2 guard

    subgraph S3["🔀 阶段3: 二级路由分发"]
        direction LR
        ROUTE_MAP["_SUB_ROUTE_MAP<br/>───────<br/>company_query<br/>→ _query_company()<br/>bidding_query<br/>→ _query_bidding()<br/>all → 多表并行"]
        THREAD["ThreadPoolExecutor<br/>跨平台超时控制<br/>node_total_timeout=45s"]
    end
    class S3 step2

    subgraph S4["🔍 阶段4: 多阶段召回 (详见 §6)"]
        direction TB
        STAGE0["Stage 0 (1.08): Milvus 语义检索<br/>mysql_price_semantic 集合"]
        STAGE1["Stage 1 (1.00): OR FULLTEXT<br/>MATCH...AGAINST IN BOOLEAN MODE"]
        STAGE2["Stage 2 (0.92): AND FULLTEXT<br/>+前缀强匹配"]
        STAGE3["Stage 3 (0.82): LIKE 回退<br/>%关键词%"]
        STAGE4["Stage 4 (0.72): 逐关键词拆分检索"]
        STAGE5["Stage 5 (0.55): 全表扫描兜底<br/>LIMIT 限制"]
        RANK["混合重排序<br/>综合权重 + MySQL得分 + 语义相似度"]
    end
    class S4 step3

    subgraph S5["🛡️ 阶段5: 后置回溯校验"]
        direction TB
        V1{"bidding_query<br/>召回结果匹配<br/>目标公司?"}
        V2{"company_query<br/>召回结果匹配<br/>目标公司?"}
        VPASS["✅ 校验通过"]
        VREJECT["❌ 盲目召回<br/>→ 触发引导话术"]
    end
    class S5 guard

    subgraph S6["📤 阶段6: 模板渲染"]
        direction TB
        FILTER["output_templates<br/>字段筛选<br/>_apply_output_template()"]
        RENDER["answer_templates<br/>自然语言渲染<br/>render_answer()"]
        FINAL["最终回答<br/>business_result + AIMessage"]
    end
    class S6 step5

    Q1 --> LLM1
    LLM1 --> PARSE
    PARSE --> POST
    POST --> INTENT
    INTENT --> G1
    G1 -->|"是(已砍除)"| REJECT
    G1 -->|"否"| G2
    G2 -->|"是"| REJECT
    G2 -->|"否"| G3
    G3 -->|"是"| REJECT
    G3 -->|"否"| G4
    G4 -->|"是"| G5
    G5 -->|"是:确定性兜底"| PASS
    G5 -->|"否"| REJECT
    G4 -->|"否"| PASS
    PASS --> ROUTE_MAP
    ROUTE_MAP --> THREAD
    THREAD --> STAGE0
    STAGE0 --> STAGE1
    STAGE1 --> STAGE2
    STAGE2 --> STAGE3
    STAGE3 --> STAGE4
    STAGE4 --> STAGE5
    STAGE5 --> RANK
    RANK --> V1
    V1 -->|"否"| VREJECT
    V1 -->|"是"| V2
    V2 -->|"否"| VREJECT
    V2 -->|"是"| VPASS
    VPASS --> FILTER
    FILTER --> RENDER
    RENDER --> FINAL
```

---

## 6. Price Inquiry — 多阶段召回与权重策略

```mermaid
graph LR
    classDef stage0 fill:#0d3b0d,stroke:#00ff88,stroke-width:2px,color:#a8ffcc,rx:15,ry:15
    classDef stage1 fill:#0a2a1a,stroke:#00cc66,stroke-width:2px,color:#a8e6cf,rx:15,ry:15
    classDef stage2 fill:#1a3a0a,stroke:#99cc00,stroke-width:2px,color:#d4ff80,rx:15,ry:15
    classDef stage3 fill:#3a2a0a,stroke:#ffaa00,stroke-width:2px,color:#ffe699,rx:15,ry:15
    classDef stage4 fill:#3a0a0a,stroke:#ff6600,stroke-width:2px,color:#ffcc99,rx:15,ry:15
    classDef stage5 fill:#4a0a0a,stroke:#ff3333,stroke-width:2px,color:#ffaaaa,rx:15,ry:15
    classDef merge fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef result fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12

    subgraph RECALL["多阶段召回流水线 — 6级权重递减"]
        direction TB

        S0["🎯 Stage 0 — 权重 1.08<br/>━━━━━━━━━━<br/>Milvus 语义向量检索<br/>mysql_price_semantic 集合<br/>BGE-m3 稠密向量<br/>Top-K=64, Threshold=0.30<br/>每表限制24条"]
        class S0 stage0

        S1["📝 Stage 1 — 权重 1.00<br/>━━━━━━━━━━<br/>OR FULLTEXT<br/>MATCH...AGAINST<br/>IN BOOLEAN MODE<br/>关键词 OR 连接"]
        class S1 stage1

        S2["📝 Stage 2 — 权重 0.92<br/>━━━━━━━━━━<br/>AND FULLTEXT<br/>+前缀强制匹配<br/>所有关键词必须命中"]
        class S2 stage2

        S3["🔍 Stage 3 — 权重 0.82<br/>━━━━━━━━━━<br/>LIKE 回退<br/>关键词逐词 %词%<br/>全表索引扫描"]
        class S3 stage3

        S4["🔍 Stage 4 — 权重 0.72<br/>━━━━━━━━━━<br/>逐关键词拆分<br/>每个关键词独立查询<br/>结果取交集"]
        class S4 stage4

        S5["⚠️ Stage 5 — 权重 0.55<br/>━━━━━━━━━━<br/>全表扫描兜底<br/>无索引覆盖<br/>LIMIT 限制返回量"]
        class S5 stage5
    end

    subgraph POST_PROCESS["后处理"]
        direction TB
        DEDUP["🔗 跨阶段去重合并<br/>基于主键/联合键"]
        RERANK["📊 Python 侧重排序<br/>综合得分 =<br/>阶段权重 × (MySQL得分 + 语义相似度)"]
        TOPN["📋 取 Top-N 结果<br/>按阈值截断"]
    end
    class POST_PROCESS merge

    FINAL_OUT["📤 排序后结果集 → 进入输出模板"]
    class FINAL_OUT result

    S0 --> DEDUP
    S1 --> DEDUP
    S2 --> DEDUP
    S3 --> DEDUP
    S4 --> DEDUP
    S5 --> DEDUP
    DEDUP --> RERANK
    RERANK --> TOPN
    TOPN --> FINAL_OUT
```

### 召回阶段触发条件与策略

| 阶段 | 权重 | 方法 | 触发条件 | 性能 |
|------|------|------|---------|------|
| **0** | 1.08 | Milvus 语义检索 | 始终执行（如有语义集合） | ⚡ 快，依赖 Milvus |
| **1** | 1.00 | OR FULLTEXT | 有关键词 + FULLTEXT索引存在 | ⚡ 快，索引覆盖 |
| **2** | 0.92 | AND FULLTEXT | Stage1 结果不足时 | ⚡ 快，更严格匹配 |
| **3** | 0.82 | LIKE 回退 | Stage2 仍不足 + 关键词有效 | 🐢 慢，全索引扫描 |
| **4** | 0.72 | 关键词拆分 | Stage3 不足 + 多关键词 | 🐢 慢，多次查询 |
| **5** | 0.55 | 全表兜底 | 极端情况（所有上级均空） | 🐌 最慢，紧急信号 |

---

## 7. Public KB — RAG 混合检索流水线

```mermaid
graph TB
    classDef input fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:12,ry:12
    classDef dense fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef sparse fill:#0d3b0d,stroke:#00ff88,stroke-width:2px,color:#a8ffcc,rx:12,ry:12
    classDef fusion fill:#3a0a3a,stroke:#c77dff,stroke-width:2px,color:#e0b3ff,rx:12,ry:12
    classDef rerank fill:#1a3a0a,stroke:#99cc00,stroke-width:2px,color:#d4ff80,rx:12,ry:12
    classDef decide fill:#2d1b00,stroke:#e76f51,stroke-width:2px,color:#ffc069,rx:12,ry:12
    classDef output fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12
    classDef degraded fill:#3a0a0a,stroke:#ff3333,stroke-width:2px,color:#ffaaaa,rx:12,ry:12

    Q["❓ 用户问题<br/>'什么是公开招标？'"]
    class Q input

    subgraph DENSE["🔵 稠密向量检索 (COSINE)"]
        direction TB
        D_EMBED["BGE-m3 向量化<br/>1024维稠密向量"]
        D_SEARCH["pymilvus.search()<br/>anns_field='vector'<br/>metric_type=COSINE<br/>nprobe=32<br/>limit=30"]
    end
    class DENSE dense

    subgraph SPARSE["🟢 稀疏向量检索 (BM25)"]
        direction TB
        S_CHECK{"Schema 包含<br/>sparse_vector?"}
        S_SEARCH["BM25 自动tokenize<br/>anns_field='sparse_vector'<br/>metric_type=IP<br/>limit=30"]
        S_SKIP["降级: 跳过稀疏路"]
    end
    class SPARSE sparse

    subgraph RR_FUSION["🟣 RRF 融合 (Reciprocal Rank Fusion)"]
        direction TB
        RRF_OP["RRFRanker(k=60)<br/>collection.hybrid_search()<br/>双路 AnnSearchRequest<br/>融合后取 Top-30"]
    end
    class RR_FUSION fusion

    subgraph RERANKER["🟡 Cross-Encoder 精排"]
        direction TB
        RERANK_API["_SiliconFlowReranker<br/>BAAI/bge-reranker-v2-m3<br/>POST /rerank<br/>query + documents<br/>Top-K=5"]
        RERANK_FALLBACK["降级: API失败<br/>→ 回退到原始排序<br/>(relevance_score=0.5)"]
    end
    class RERANKER rerank

    subgraph THRESHOLD["🔴 动态阈值过滤"]
        direction TB
        ADAPT["_adaptive_threshold(top_score)<br/>────────<br/>top1 ≥ 0.75 → 阈值 0.40<br/>top1 ≥ 0.50 → 阈值 0.45<br/>top1 < 0.50 → 阈值 0.50"]
        FILTER["过滤低于阈值的文档"]
    end
    class THRESHOLD decide

    subgraph FINAL["📤 最终输出"]
        direction TB
        SOURCE_CHECK{"有结果?"}
        REFUSE_OUT["❌ 拒答<br/>'公共知识库中暂无相关内容'<br/>sources: []"]
        LLM_GEN["✅ LLM 生成<br/>prompt → deepseek-chat<br/>→ StrOutputParser<br/>────────<br/>回答 + 引用来源"]
    end
    class FINAL output

    DEGRADED_FULL["⚠️ 纯稠密降级路径<br/>_dense_only_retrieve()<br/>────────<br/>优先 pymilvus 原生搜索<br/>(获取动态元数据)<br/>失败 → langchain_milvus<br/>similarity_search_with_score"]
    class DEGRADED_FULL degraded

    Q --> D_EMBED
    Q --> S_CHECK
    D_EMBED --> D_SEARCH
    S_CHECK -->|"✅"| S_SEARCH
    S_CHECK -->|"❌"| S_SKIP
    D_SEARCH --> RRF_OP
    S_SEARCH --> RRF_OP
    S_SKIP -.->|"跳过"| DEGRADED_FULL
    RRF_OP -->|"有结果"| RERANK_API
    RRF_OP -->|"异常"| DEGRADED_FULL
    RERANK_API -->|"成功"| ADAPT
    RERANK_API -->|"失败"| RERANK_FALLBACK
    RERANK_FALLBACK --> ADAPT
    ADAPT --> FILTER
    FILTER --> SOURCE_CHECK
    DEGRADED_FULL --> SOURCE_CHECK
    SOURCE_CHECK -->|"否"| REFUSE_OUT
    SOURCE_CHECK -->|"是"| LLM_GEN
```

---

## 8. Public KB — PDF 入库全流程

```mermaid
graph TB
    classDef input fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:12,ry:12
    classDef parse fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef clean fill:#0d3b0d,stroke:#00ff88,stroke-width:2px,color:#a8ffcc,rx:12,ry:12
    classDef chunk fill:#1a3a0a,stroke:#99cc00,stroke-width:2px,color:#d4ff80,rx:12,ry:12
    classDef embed fill:#3a0a3a,stroke:#c77dff,stroke-width:2px,color:#e0b3ff,rx:12,ry:12
    classDef store fill:#2d1b00,stroke:#f4a261,stroke-width:2px,color:#ffd166,rx:12,ry:12
    classDef done fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12

    subgraph S1["📥 步骤1: 文件扫描"]
        PDF_DIR["raw_pdfs/ 目录<br/>3本法规PDF (~2000+页)<br/>sorted(*.pdf)"]
    end
    class S1 input

    subgraph S2["📑 步骤2: MinerU 解析"]
        direction TB
        CACHE_CHECK{"缓存.md<br/>已存在?"}
        CACHE_LOAD["直接读取缓存<br/>跳过解析"]
        MINERU_RUN["subprocess: magic-pdf<br/>-p pdf_path<br/>-o output_dir<br/>timeout=3600s"]
        MD_OUT["输出: 结构化Markdown<br/>保留完整标题层级"]
    end
    class S2 parse

    subgraph S3["🧹 步骤3: 文本清洗 — TextCleaner.clean()"]
        direction TB
        R1["① 去页眉页脚<br/>频次统计: ≥5次出现<br/>且长度 <80字符 → 移除"]
        R2["② 去页码<br/>独立纯数字行 → 移除"]
        R3["③ 去过短行<br/><10字符的非标题行 → 移除"]
        R4["④ 压缩空行<br/>连续3+空行 → 压缩为2个"]
    end
    class S3 clean

    subgraph S4["✂️ 步骤4: 语义切片 — SemanticChunker.chunk()"]
        direction TB
        C1["主策略: 按 #/##/###<br/>标题层级拆分<br/>保留章节路径<br/>'第一章 > 第一节'"]
        C2["补策略: 超长块<br/>按句子边界二次拆分<br/>中文句号/问号/感叹号<br/>max_chars=2000<br/>overlap_chars=100"]
    end
    class S4 chunk

    subgraph S5["🧬 步骤5: 向量化"]
        direction TB
        EMBED_CALL["_SafeEmbeddings<br/>BGE-m3 (1024维)<br/>超长文本自动截断<br/>batch_size=100"]
    end
    class S5 embed

    subgraph S6["🐘 步骤6: 入库 Milvus"]
        direction TB
        DROP_OLD["_drop_if_exists()<br/>删除旧集合"]
        CREATE_COL["create_schema()<br/>id(INT64,auto_id)<br/>text(VARCHAR 65535)<br/>vector(FLOAT 1024维)<br/>+ 动态字段"]
        INDEX["create_index()<br/>IVF_FLAT + COSINE<br/>nlist=256"]
        BATCH_INSERT["_batch_insert()<br/>逐批向量化 + 插入<br/>batch_size=100"]
        FLUSH["flush() + load_collection()"]
    end
    class S6 store

    subgraph S7["✅ 完成"]
        QA_BUILD["_build_qa_chain()<br/>构建 LCEL 问答链"]
        READY["PublicKnowledgeRAG<br/>就绪，可查询"]
    end
    class S7 done

    PDF_DIR --> CACHE_CHECK
    CACHE_CHECK -->|"是"| CACHE_LOAD
    CACHE_CHECK -->|"否"| MINERU_RUN
    CACHE_LOAD --> MD_OUT
    MINERU_RUN --> MD_OUT
    MD_OUT --> R1
    R1 --> R2
    R2 --> R3
    R3 --> R4
    R4 --> C1
    C1 --> C2
    C2 --> EMBED_CALL
    EMBED_CALL --> DROP_OLD
    DROP_OLD --> CREATE_COL
    CREATE_COL --> INDEX
    INDEX --> BATCH_INSERT
    BATCH_INSERT --> FLUSH
    FLUSH --> QA_BUILD
    QA_BUILD --> READY
```

---

## 9. Answer Templates — 回答模板渲染引擎

```mermaid
graph TB
    classDef input fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:12,ry:12
    classDef logic fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef single fill:#0d3b0d,stroke:#00ff88,stroke-width:2px,color:#a8ffcc,rx:12,ry:12
    classDef multi fill:#1a3a0a,stroke:#99cc00,stroke-width:2px,color:#d4ff80,rx:12,ry:12
    classDef empty fill:#3a0a0a,stroke:#ff6600,stroke-width:2px,color:#ffcc99,rx:12,ry:12
    classDef notfound fill:#4a0a0a,stroke:#ff3333,stroke-width:2px,color:#ffaaaa,rx:12,ry:12
    classDef output fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12

    subgraph IN["📥 输入"]
        direction LR
        PARAM1["query_type: 'company_detail'"]
        PARAM2["records: [{company_name, ...}]"]
        PARAM3["entity: '华为技术有限公司'"]
    end
    class IN input

    RESOLVE["🔍 _resolve_template(query_type)<br/>→ ANSWER_TEMPLATES 字典查找<br/>────────<br/>8种模板:<br/>company_detail | company_industry<br/>penalty_check | bidder_query<br/>project_detail | purchaser_query<br/>aggregation | mixed"]
    class RESOLVE logic

    CHECK{"records<br/>数量?"}
    class CHECK logic

    subgraph S["单条记录"]
        CTX1["_build_record_context(record)<br/>列名 → 模板占位符<br/>金额格式化(_fmt_amount)<br/>空值 → '未提供'"]
        R1["_render_single()<br/>template.single_template.format()"]
        EX1["'经查询，华为技术有限公司<br/>是一家成立于1987年的<br/>有限责任公司...<br/>(数据来源: ztb_clean.company_info)'"]
    end
    class S single

    subgraph M["多条记录"]
        CTX2["逐条 _build_record_context()<br/>附加 index 序号"]
        R2["_render_multi()<br/>template.multi_template.format()<br/>template.item_line 逐条渲染"]
        EX2["'华为技术有限公司 共中标 5 个项目:<br/>【1】2024-06-15 | 某单位「项目A」| ...<br/>【2】2024-05-20 | ...<br/>(数据来源: ztb_clean.bid_project)'"]
    end
    class M multi

    subgraph E["空结果"]
        R3["_render_empty()<br/>template.empty_template.format(entity)<br/>含: 可能原因 + 下一步建议"]
        EX3["'在系统收录的 17,742 条记录中<br/>暂未查询到'XX公司'的中标信息。<br/>这可能因为: ① ...<br/>建议: 查工商信息 / 查不良记录'"]
    end
    class E empty

    subgraph NF["实体不存在"]
        R4["_render_not_found()<br/>template.not_found_template.format()<br/>含: 可能原因(3条) + 重试建议"]
        EX4["'系统中未收录'XX公司'的<br/>不良记录信息。这可能因为:<br/>① 该公司确无处罚记录<br/>② 系统暂未收录<br/>③ 公司名称存在差异'"]
    end
    class NF notfound

    FINAL["📤 返回自然语言回答文本"]
    class FINAL output

    IN --> RESOLVE
    RESOLVE --> CHECK
    CHECK -->|"1条"| S
    CHECK -->|"≥2条"| M
    CHECK -->|"0条 + 模板有not_found"| NF
    CHECK -->|"0条"| E
    S --> FINAL
    M --> FINAL
    E --> FINAL
    NF --> FINAL
```

---

## 10. Output Templates — 输出字段筛选框架

```mermaid
graph TB
    classDef registry fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef template fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12
    classDef logic fill:#3a0a3a,stroke:#c77dff,stroke-width:2px,color:#e0b3ff,rx:12,ry:12
    classDef flow fill:#1a3a5c,stroke:#1890ff,stroke-width:1.5px,color:#91caff,rx:10,ry:10
    classDef output fill:#2d1b00,stroke:#f4a261,stroke-width:2px,color:#ffd166,rx:12,ry:12

    subgraph REG["🗂️ 全局字段注册表 _FIELD_REGISTRY"]
        direction LR
        FD1["company_name<br/>企业名称<br/>required"]
        FD2["credit_code<br/>统一社会信用代码"]
        FD3["winning_amount<br/>中标金额<br/>required"]
        FD4["project_number<br/>项目编号<br/>required"]
        FD5["business_scope<br/>经营范围<br/>max_chars=200"]
        FD6["... 共30+字段"]
    end
    class REG registry

    subgraph TPL["📋 输出模板定义 (8种)"]
        direction TB
        T1["company_detail<br/>required: company_name, credit_code, business_status<br/>conditional: penalty_date, illegal_behavior, penalty_result<br/>optional: legal_person, registered_capital, ..."]
        T2["bidder_query<br/>required: project_name, project_number, purchaser, successful_bidder, winning_amount, winning_date<br/>optional: subject_matter, agent, ..."]
        T3["project_detail<br/>required: project_name, project_number, purchaser, successful_bidder, winning_amount, budget_amount, winning_date, agent, subject_matter"]
        T4["... 5种更多模板"]
    end
    class TPL template

    subgraph ENGINE["⚙️ _apply_output_template() 执行流程"]
        direction TB
        E1["① 确定活跃字段集<br/>required ∪ (满足条件的 conditional) ∪ optional"]
        E2["② 逐字段空值处理<br/>null_behavior: hide | show_placeholder | keep_null"]
        E3["③ 逐字段截断<br/>max_chars → 追加 '…'"]
        E4["④ 按 display_order 排序"]
        E5["⑤ 字段数上限裁剪<br/>超 max_fields_per_record(12)<br/>从 optional 尾部删除"]
        E6["⑥ 过滤全NULL记录"]
    end
    class ENGINE logic

    subgraph FLOW["示例: company_detail 查询 → 输出"]
        RAW["原始SQL结果<br/>{'company_name':'华为', 'credit_code':'914403...', 'business_scope':'计算机软硬件...(200字)', 'legal_person':'赵某', 'registered_capital':'4038718.0833万', 'establish_date':datetime(1987,9,15), 'address':None, ...}"]
        FILTERED["筛选后<br/>{'企业名称':'华为', '统一社会信用代码':'914403...', '法定代表人':'赵某', '注册资本':'4038718.08万', '成立日期':'1987-09-15', '经营状态':'存续', '所属行业':'软件信息', '企业类型':'有限责任公司', '经营范围':'计算机软硬件...'}<br/># address 因null被hide<br/># 超过10字段,尾部optional裁剪"]
    end
    class FLOW flow

    FORMATTED["📤 格式化记录列表<br/>→ 传入 answer_templates 渲染"]
    class FORMATTED output

    REG -.- TPL
    TPL --> E1
    E1 --> E2
    E2 --> E3
    E3 --> E4
    E4 --> E5
    E5 --> E6
    RAW --> E1
    E6 --> FILTERED
    FILTERED --> FORMATTED
```

---

## 11. Fallback — 全局异常兜底机制

```mermaid
graph TB
    classDef normal fill:#0d3b0d,stroke:#00ff88,stroke-width:2px,color:#a8ffcc,rx:12,ry:12
    classDef error fill:#4a0a0a,stroke:#ff3333,stroke-width:2px,color:#ffaaaa,rx:12,ry:12
    classDef wrap fill:#2d1b00,stroke:#e76f51,stroke-width:2px,color:#ffc069,rx:15,ry:15
    classDef result fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12

    subgraph WRAPPER["🛡️ _with_fallback(node_fn) 装饰器"]
        direction TB

        subgraph NORMAL_PATH["✅ 正常路径"]
            direction TB
            CALL["调用原始节点<br/>result = node_fn(state)"]
            RETURN["返回正常结果<br/>{business_result, messages}"]
        end
        class NORMAL_PATH normal

        subgraph ERROR_PATH["❌ 异常降级路径"]
            direction TB
            CATCH["捕获任何 Exception"]
            LOG_ERR["logger.error(...)<br/>记录节点名 + 异常栈"]
            BUILD_MSG["构造友好降级消息:<br/>'抱歉，「{节点中文名}」功能<br/>暂时不可用，请稍后重试...<br/>您可以:<br/>📚 咨询招投标法规知识<br/>💰 查询历史中标价格<br/>💬 进行通用对话'"]
            RETURN_ERR["返回降级结果:<br/>branch='fallback'<br/>answer=降级消息<br/>data={error, failed_branch}"]
        end
        class ERROR_PATH error
    end
    class WRAPPER wrap

    subgraph STATE_FLOW["StateGraph 中的注册"]
        direction LR
        REG1["graph.add_node(<br/>  'knowledge_qa',<br/>  _with_fallback(<br/>    node_knowledge_qa))"]
        REG2["graph.add_node(<br/>  'price_inquiry',<br/>  _with_fallback(<br/>    node_price_inquiry))"]
        REG3["graph.add_node(<br/>  'general_chat',<br/>  _with_fallback(<br/>    node_general_chat))"]
        REG4["graph.add_node(<br/>  'doc_qa',<br/>  _with_fallback(<br/>    node_doc_qa))"]
    end
    class STATE_FLOW wrap

    subgraph FALLBACK_NODE["🔄 fallback 节点处理"]
        direction TB
        CHECK_SRC{"business_result<br/>含 failed_branch?"}
        FROM_WRAP["来自 _with_fallback<br/>直接透传降级消息"]
        FROM_ROUTER["来自 Router 兜底<br/>意图不明<br/>列出可用功能清单"]
    end
    class FALLBACK_NODE result

    CALL --> RETURN
    CALL -.->|"异常"| CATCH
    CATCH --> LOG_ERR
    LOG_ERR --> BUILD_MSG
    BUILD_MSG --> RETURN_ERR
    RETURN_ERR --> CHECK_SRC
    RETURN --> CHECK_SRC
    CHECK_SRC -->|"是"| FROM_WRAP
    CHECK_SRC -->|"否"| FROM_ROUTER
```

---

## 12. Checkpointer — 对话记忆持久化

```mermaid
graph TB
    classDef factory fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef current fill:#0d3b0d,stroke:#00ff88,stroke-width:2px,color:#a8ffcc,rx:12,ry:12
    classDef planned fill:#1a3a0a,stroke:#99cc00,stroke-width:2px,color:#d4ff80,rx:12,ry:12
    classDef future fill:#3a2a0a,stroke:#ffaa00,stroke-width:2px,color:#ffe699,rx:12,ry:12
    classDef code fill:#1a1a2e,stroke:#c77dff,stroke-width:1.5px,color:#e0b3ff,rx:10,ry:10

    FACTORY["🏭 create_checkpointer(backend, connection_string)<br/>抽象工厂 — 一行代码切换后端"]
    class FACTORY factory

    subgraph NOW["🟢 Demo 阶段 (当前)"]
        MEM["MemorySaver<br/>进程内存<br/>───────<br/>✅ 零配置<br/>❌ 重启丢失<br/>❌ 无法扩展"]
    end
    class NOW current

    subgraph SINGLE["🟡 单机部署"]
        SQLITE["SqliteSaver<br/>本地文件<br/>───────<br/>✅ 零依赖服务<br/>✅ 持久化<br/>✅ 单文件部署<br/>❌ 单机限制"]
    end
    class SINGLE planned

    subgraph CLUSTER["🔵 集群部署"]
        PG["PostgresSaver<br/>PostgreSQL<br/>───────<br/>✅ 生产级持久化<br/>✅ 多实例共享<br/>✅ 高可用<br/>❌ 需维护 PG 实例"]
        REDIS["RedisSaver<br/>Redis<br/>───────<br/>✅ 高性能<br/>✅ 多实例共享<br/>✅ 自动过期<br/>❌ 需维护 Redis 实例"]
    end
    class CLUSTER future

    subgraph INTEGRATION["📋 集成方式 (业务代码零改动)"]
        CODE["# Demo 阶段<br/>agent = AgentGraph(checkpointer_backend='memory')<br/><br/># 未来单机<br/>agent = AgentGraph(<br/>    checkpointer_backend='sqlite',<br/>    connection_string='checkpoints.db')<br/><br/># 未来集群<br/>agent = AgentGraph(<br/>    checkpointer_backend='postgres',<br/>    connection_string='postgresql://...')"]
    end
    class INTEGRATION code

    FACTORY --> MEM
    FACTORY --> SQLITE
    FACTORY --> PG
    FACTORY --> REDIS
    MEM -.- INTEGRATION
    SQLITE -.- INTEGRATION
    PG -.- INTEGRATION
    REDIS -.- INTEGRATION
```

---

## 13. 数据源协同策略

```mermaid
graph TB
    classDef user fill:#16213e,stroke:#0f3460,stroke-width:2px,color:#c0caf5,rx:12,ry:12
    classDef router fill:#3a0a3a,stroke:#ff6b6b,stroke-width:2px,color:#ffc0cb,rx:12,ry:12
    classDef knowledge fill:#0f3460,stroke:#1677ff,stroke-width:2px,color:#a8d8ff,rx:12,ry:12
    classDef price fill:#162447,stroke:#1b998b,stroke-width:2px,color:#a8e6cf,rx:12,ry:12
    classDef mysql fill:#2d1b00,stroke:#f4a261,stroke-width:2px,color:#ffd166,rx:12,ry:12
    classDef milvus fill:#0d3b0d,stroke:#00ff88,stroke-width:2px,color:#a8ffcc,rx:12,ry:12

    USER["👤 用户提问"]
    class USER user

    R["🔀 Router<br/>意图分类"]
    class R router

    subgraph KW["📚 knowledge_qa 路径<br/>专业知识问答"]
        direction TB
        KW_Q["问题: '公开招标和邀请招标有什么区别？'"]
        KW_M["Milvus public_kb 集合<br/>━━━━━━━━━━<br/>稠密 + 稀疏混合检索<br/>RRF + Reranker<br/>法规PDF语义匹配"]
        KW_LLM["DeepSeek LLM<br/>生成专业回答 + 溯源引用"]
        KW_A["回答: 两种招标方式的详细对比<br/>附带: 《招投标法》第X条原文"]
    end
    class KW knowledge

    subgraph PI["💰 price_inquiry 路径<br/>智能询价"]
        direction TB
        PI_Q["问题: '查一下智慧交通项目的中标价格'"]
        PI_S0["Milvus mysql_price_semantic<br/>━━━━━━━━━━<br/>结构化数据语义召回<br/>匹配 purchasers/bidders"]
        PI_S1["MySQL ztb_clean<br/>━━━━━━━━━━<br/>FULLTEXT 精确检索<br/>bid_project 表<br/>stage 1-5 多级回退"]
        PI_R["Python 侧重排序<br/>综合权重 + 得分"]
        PI_A["回答: 中标记录详情<br/>含公司名、金额、日期<br/>(数据来源: ztb_clean.bid_project)"]
    end
    class PI price

    subgraph MYSQL_DETAIL["🐬 MySQL ztb_clean 结构"]
        direction LR
        T1["company_info<br/>38,911 行<br/>企业工商信息"]
        T2["company_penalty<br/>1,805 行<br/>企业处罚记录"]
        T3["bid_project<br/>17,742 行<br/>招标项目信息"]
        T4["product_info<br/>产品信息"]
    end
    class MYSQL_DETAIL mysql

    subgraph MILVUS_DETAIL["🐘 Milvus 集合"]
        direction LR
        C1["public_kb<br/>法规知识库<br/>IVF_FLAT / COSINE<br/>1024维"]
        C2["mysql_price_semantic<br/>结构化语义回召<br/>BGE-m3 向量化<br/>每行→语义文档"]
    end
    class MILVUS_DETAIL milvus

    USER --> R
    R -->|"knowledge_qa"| KW_Q
    R -->|"price_inquiry"| PI_Q
    KW_Q --> KW_M
    KW_M --> KW_LLM
    KW_LLM --> KW_A
    KW_M -.- C1
    KW_LLM -.- MILVUS_DETAIL

    PI_Q --> PI_S0
    PI_S0 --> PI_S1
    PI_S1 --> PI_R
    PI_R --> PI_A
    PI_S0 -.- C2
    PI_S1 -.- T3
    PI_S1 -.- T1
    PI_S1 -.- T2
```

---

## 附录：关键代码路径速查

| 组件 | 文件 | 关键行/函数 |
|------|------|-----------|
| StateGraph 构建 | `agent/graph.py` | L108-184 `build_graph()` |
| 全局异常兜底 | `agent/graph.py` | L51-92 `_with_fallback()` |
| 意图分类 | `agent/router.py` | L159-212 `_route_via_tool_calling()` |
| State 定义 | `agent/state.py` | L19-37 `AgentState` |
| 统一意图解析 | `agent/nodes/price_inquiry.py` | L577-617 `_parse_unified_intent()` |
| 多阶段召回权重 | `agent/nodes/price_inquiry.py` | L116-123 `_RECALL_STAGE_WEIGHTS` |
| P0-11 公司名校验 | `agent/nodes/price_inquiry.py` | L472-504 `_is_valid_company_name()` |
| P0-12 项目编号提取 | `agent/nodes/price_inquiry.py` | L427-468 `_extract_project_number_candidate()` |
| 回答模板引擎 | `agent/nodes/answer_templates.py` | L377-427 `render_answer()` |
| 输出字段筛选 | `agent/nodes/output_templates.py` | L264-337 `_apply_output_template()` |
| LCEL 问答链 | `public_kb/qa_chain.py` | L106-310 `build_qa_chain()` |
| 混合检索逻辑 | `public_kb/qa_chain.py` | L157-270 `_retrieve()` |
| 自适应阈值 | `public_kb/qa_chain.py` | L368-380 `_adaptive_threshold()` |
| Reranker 客户端 | `public_kb/qa_chain.py` | L317-365 `_SiliconFlowReranker` |
| Milvus 集合管理 | `public_kb/milvus_store.py` | L26-200 `MilvusStoreManager` |
| 安全 Embedding | `public_kb/embedding_service.py` | L28-50 `_SafeEmbeddings` |
| 全局配置 | `public_kb/config.py` | L22-176 `Settings` |
| Checkpointer 工厂 | `agent/checkpointer.py` | L20-83 `create_checkpointer()` |
